(() => {
  'use strict';
  if(!window.App)return;
  const baseToast=App.toast;const baseIcon=App.icon;const baseRenderSettings=App.renderSettings;const baseSaveSettings=App.saveSettings;

  Object.assign(App, {
    icon(name){if(name==='notifications'){return '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M10 21h4"/></svg>';}return baseIcon.call(this,name);},
    notificationTitle(type){return type==='error'?'Atenção':type==='success'?'Concluído':'Informação';},
    async recordInteractionNotification(message,type=''){
      if(!message||!window.AppDB)return;const now=Date.now();const latest=this.state.notifications?.[0];if(latest&&latest.message===message&&now-new Date(latest.createdAt).getTime()<2500)return;
      const hash=location.hash||'#/dashboard';const params=hash.includes('?')?`?${hash.split('?').slice(1).join('?')}`:'';const item={id:crypto.randomUUID(),title:this.notificationTitle(type),message:String(message),type:type||'info',route:this.state.route||'dashboard',params,createdAt:new Date(now).toISOString(),readAt:null};
      await AppDB.put('notifications',item);this.state.notifications=[item,...(this.state.notifications||[])];for(const old of this.state.notifications.slice(200)){await AppDB.remove('notifications',old.id).catch(()=>{});}this.state.notifications=this.state.notifications.slice(0,200);this.updateNotificationBadge();
    },
    shouldAttentionFeedback(message,type){if(type==='error')return true;if(type!=='success')return false;return /guardad|criad|atualiz|arquivad|reabert|enviado|restaur|backup|snapshot|configura|eliminad|removid/i.test(String(message));},
    async playAttentionFeedback(kind='success'){
      const prefs=this.state.settings?.attentionFeedback||{};
      if(prefs.vibration!==false&&navigator.vibrate){try{navigator.vibrate(kind==='error'?[70,50,70]:60);}catch{} }
      if(prefs.sound===false)return;const AudioCtx=window.AudioContext||window.webkitAudioContext;if(!AudioCtx)return;
      try{const ctx=new AudioCtx();if(ctx.state==='suspended')await ctx.resume();const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';osc.frequency.value=kind==='error'?240:820;gain.gain.setValueAtTime(0.0001,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.07,ctx.currentTime+0.012);gain.gain.exponentialRampToValueAtTime(0.0001,ctx.currentTime+0.11);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.12);setTimeout(()=>ctx.close().catch(()=>{}),180);}catch{}
    },
    attentionReminders(){
      const prefs=this.state.settings?.attentionFeedback||{};if(prefs.reminders===false)return[];const minutes=Math.min(240,Math.max(5,Number(prefs.reminderMinutes||15)));const cutoff=Date.now()-minutes*60000;const reminders=[];
      for(const r of this.state.records||[]){if(r.archived||new Date(r.updatedAt||r.createdAt).getTime()>cutoff)continue;const base={id:r.id,displayId:r.displayId||'Rascunho',client:r.clientName||r.establishmentName||'Sem cliente',updatedAt:r.updatedAt||r.createdAt};if(r.status==='DRAFT')reminders.push({...base,kind:'draft',title:'Rascunho por concluir',text:`Sem alterações há pelo menos ${minutes} min.`});else if(r.emailSent==='PENDING')reminders.push({...base,kind:'email',title:'E-mail por concluir',text:'O registo continua marcado como “Por enviar”.'});else if(r.status==='WAITING_RESPONSE')reminders.push({...base,kind:'waiting',title:'Seguimento pendente',text:'O registo continua a aguardar resposta.'});}
      return reminders.sort((a,b)=>new Date(a.updatedAt)-new Date(b.updatedAt)).slice(0,12);
    },
    updateNotificationBadge(){const badge=this.els?.notificationBadge;const button=this.els?.notificationButton;if(!badge)return;const unread=(this.state.notifications||[]).filter(n=>!n.readAt).length;const total=unread+this.attentionReminders().length;badge.hidden=total===0;badge.textContent=total>99?'99+':String(total);if(button)button.setAttribute('aria-label',total?`Abrir notificações — ${total} por verificar`:'Abrir notificações');},
    notificationItemHtml(n){return `<article class="notification-row ${n.readAt?'':'unread'}"><span class="notification-dot" aria-hidden="true"></span><div class="notification-copy"><div><strong>${this.escape(n.title||'Informação')}</strong><time>${this.formatDateTimeCompact(n.createdAt)}</time></div><p>${this.escape(n.message||'')}</p></div><div class="notification-actions">${n.route?`<button class="btn btn-secondary btn-small" type="button" data-notification-open="${this.escapeAttr(n.id)}">Abrir</button>`:''}<button class="btn btn-ghost btn-small" type="button" data-notification-delete="${this.escapeAttr(n.id)}">Eliminar</button></div></article>`;},
    renderNotifications(){
      const reminders=this.attentionReminders();const messages=this.state.notifications||[];const unread=messages.filter(n=>!n.readAt).length;
      this.els.viewContainer.innerHTML=`<div class="page-head"><div><p class="eyebrow">Centro de atenção</p><h3>Notificações e pendências</h3></div><div class="page-actions"><button class="btn btn-secondary" type="button" data-notifications-read-all ${unread?'':'disabled'}>Marcar como lidas</button><button class="btn btn-secondary" type="button" data-notifications-clear ${messages.length?'':'disabled'}>Limpar mensagens</button></div></div><div class="notification-summary"><span><strong>${reminders.length}</strong> a concluir</span><span><strong>${unread}</strong> mensagens por ler</span><span><strong>${messages.length}</strong> interações guardadas</span></div><section class="panel"><div class="panel-head"><div><h3>A concluir</h3><span class="muted">Lembretes gerados pelos registos locais; não são enviados para servidores.</span></div></div><div class="panel-body">${reminders.length?`<div class="reminder-list">${reminders.map(r=>`<article class="reminder-card"><div><span class="reminder-kind">${this.escape(r.kind==='draft'?'Rascunho':r.kind==='email'?'Comunicação':'Seguimento')}</span><strong>${this.escape(r.title)}</strong><p>${this.escape(r.displayId)} · ${this.escape(r.client)}</p><small>${this.escape(r.text)}</small></div><button class="btn btn-primary btn-small" type="button" data-reminder-open="${this.escapeAttr(r.id)}" data-reminder-kind="${this.escapeAttr(r.kind)}">Continuar</button></article>`).join('')}</div>`:this.empty('Sem pendências antigas.','Os rascunhos em edição recente não geram alerta.')}</div></section><section class="panel notifications-panel"><div class="panel-head"><div><h3>Mensagens das interações</h3><span class="muted">Confirmações, avisos e erros apresentados durante a utilização.</span></div></div><div class="panel-body">${messages.length?`<div class="notification-list">${messages.map(n=>this.notificationItemHtml(n)).join('')}</div>`:this.empty('Sem mensagens guardadas.','As próximas interações relevantes aparecerão aqui.')}</div></section>`;
      document.querySelectorAll('[data-notification-open]').forEach(btn=>btn.addEventListener('click',async()=>{const item=this.state.notifications.find(n=>n.id===btn.dataset.notificationOpen);if(!item)return;await this.markNotificationRead(item.id);this.navigate(item.route||'dashboard',item.params||'');}));
      document.querySelectorAll('[data-notification-delete]').forEach(btn=>btn.addEventListener('click',()=>this.deleteNotification(btn.dataset.notificationDelete)));
      document.querySelectorAll('[data-reminder-open]').forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.reminderOpen;const kind=btn.dataset.reminderKind;if(kind==='draft')this.navigate('edit',`?id=${encodeURIComponent(id)}`);else if(kind==='email')this.navigate('routing');else this.navigate('records');}));
      document.querySelector('[data-notifications-read-all]')?.addEventListener('click',()=>this.markAllNotificationsRead());
      document.querySelector('[data-notifications-clear]')?.addEventListener('click',()=>this.confirm('Limpar mensagens','As mensagens do centro de notificações serão removidas. Registos e histórico de atividade não serão afetados.',()=>this.clearNotifications()));
    },
    async markNotificationRead(id){const item=this.state.notifications.find(n=>n.id===id);if(!item||item.readAt)return;if(!item.readAt){item.readAt=new Date().toISOString();await AppDB.put('notifications',item);}this.updateNotificationBadge();},
    async markAllNotificationsRead(){const now=new Date().toISOString();for(const item of this.state.notifications){if(!item.readAt){item.readAt=now;await AppDB.put('notifications',item);}}this.updateNotificationBadge();baseToast.call(this,'Mensagens marcadas como lidas.','success');this.renderNotifications();},
    async deleteNotification(id){await AppDB.remove('notifications',id);this.state.notifications=this.state.notifications.filter(n=>n.id!==id);this.updateNotificationBadge();baseToast.call(this,'Mensagem eliminada.','success');this.renderNotifications();},
    async clearNotifications(){await AppDB.clear('notifications');this.state.notifications=[];this.updateNotificationBadge();baseToast.call(this,'Mensagens do centro de notificações limpas.','success');this.renderNotifications();},
    injectAttentionSettings(){
      const sticky=this.els.viewContainer.querySelector('.sticky-settings-save');if(!sticky||document.getElementById('attentionSettings'))return;const prefs=this.state.settings.attentionFeedback||{};const vibrationSupport=Boolean(navigator.vibrate);
      sticky.insertAdjacentHTML('beforebegin',`<section class="panel settings-section" id="attentionSettings"><div class="panel-head"><div><h3>Atenção, som e vibração</h3><span class="muted">Feedback local para confirmar ações e lembrar trabalho por concluir</span></div></div><div class="panel-body attention-settings-grid"><label class="toggle-row"><input id="attentionSound" type="checkbox" ${prefs.sound!==false?'checked':''}><span>Som curto em ações concluídas/guardadas</span></label><label class="toggle-row"><input id="attentionVibration" type="checkbox" ${prefs.vibration!==false?'checked':''} ${vibrationSupport?'':'disabled'}><span>Vibração quando suportada pelo dispositivo</span></label><label class="toggle-row"><input id="attentionReminders" type="checkbox" ${prefs.reminders!==false?'checked':''}><span>Lembrar rascunhos e seguimentos pendentes</span></label><label class="field"><span>Lembrar após</span><input id="attentionReminderMinutes" type="number" min="5" max="240" value="${Math.min(240,Math.max(5,Number(prefs.reminderMinutes||15)))}"><small class="field-hint">Minutos sem alteração antes de surgir em “A concluir”.</small></label><div class="attention-test"><button class="btn btn-secondary" type="button" data-attention-test>Testar som/vibração</button><small class="muted">O som depende das permissões do navegador. ${vibrationSupport?'Este dispositivo expõe suporte a vibração.':'Vibração não disponível neste navegador.'}</small></div></div></section>`);
      document.querySelector('[data-attention-test]')?.addEventListener('click',()=>{this.playAttentionFeedback('success');baseToast.call(this,'Teste de atenção executado.','success');});
    },
  });

  App.toast=function(message,type=''){baseToast.call(this,message,type);this.recordInteractionNotification(message,type).catch(()=>{});if(this.shouldAttentionFeedback(message,type))this.playAttentionFeedback(type==='error'?'error':'success');};
  App.renderSettings=async function(){await baseRenderSettings.call(this);this.injectAttentionSettings();};
  App.saveSettings=async function(){const sound=document.getElementById('attentionSound');const vibration=document.getElementById('attentionVibration');const reminders=document.getElementById('attentionReminders');const minutes=document.getElementById('attentionReminderMinutes');if(sound&&vibration&&reminders&&minutes){this.state.settings.attentionFeedback={sound:sound.checked,vibration:vibration.checked,reminders:reminders.checked,reminderMinutes:Math.min(240,Math.max(5,Number(minutes.value||15)))};}return baseSaveSettings.call(this);};
})();
